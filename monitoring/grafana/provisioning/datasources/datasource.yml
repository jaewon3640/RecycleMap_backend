apiVersion: 1

# Grafana 기동 시 Prometheus 데이터소스를 자동 등록 (수동 설정 불필요)
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # 같은 docker 네트워크의 prometheus 컨테이너
    isDefault: true
    editable: true
